var n : string = "";

n = 'Robb';

var isDone: boolean = false;
isDone = true;

var count : number = 5;

var names : string[] = ["Jon", "Mike"];

enum Starks {Jon, Bran, Eddard, Catlyn};
var cat : Starks = Starks.Catlyn;

function getName() : string {
  return "Brad";
}
